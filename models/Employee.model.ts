// import { Position } from "@/models/Position.model";

export interface Employee {
  firstName: string;
  lastName: string;
  positions?: any[];
  imgUrl: string;
  instagramUrl?: string;
  twitterUrl?: string;
}
